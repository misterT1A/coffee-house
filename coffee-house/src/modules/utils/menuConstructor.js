import MenuElementCreator from './menuElementCreator';

export default class MenuConstructor {
  constructor(container, product) {
    this.container = container;
    this.product = product;
  }
}
